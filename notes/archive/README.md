# Archived design material

Files in this directory are superseded proposals retained only for historical context.

They may contain:

- manifest fields the current parser does not accept;
- component types that are not implemented;
- old names and package layouts;
- roadmap ideas that were never committed to.

Do not use these files as examples or implementation references. See the [internal design index](../README.md), public documentation, and current parser instead.
